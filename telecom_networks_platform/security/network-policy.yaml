apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: telecom-networks-ingress-only
  namespace: telecom-networks
spec:
  podSelector:
    matchLabels:
      app: telecom-networks-backend
  policyTypes: [Ingress, Egress]
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              name: ingress-nginx
      ports:
        - protocol: TCP
          port: 8000
  egress:
    - to:
        - ipBlock:
            cidr: 0.0.0.0/0
      ports:
        - protocol: TCP
          port: 443

