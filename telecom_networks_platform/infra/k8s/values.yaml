image:
  repository: <ECR_REPOSITORY_URL>
  tag: latest
service:
  port: 80
ingress:
  enabled: true
  className: nginx
  hosts:
    - host: telecom_networks.quasim.example.com
      paths:
        - path: /
          pathType: Prefix
  tls:
    - hosts:
        - telecom_networks.quasim.example.com
      secretName: telecom-networks-tls
resources:
  requests:
    cpu: 500m
    memory: 1Gi
  limits:
    cpu: 2
    memory: 4Gi

