{
  "title": "Telecom Networks Kernel Observability",
  "panels": [
    {
      "type": "graph",
      "title": "Kernel p95 Latency",
      "targets": [
        {
          "expr": "histogram_quantile(0.95, sum(rate(telecom_networks_request_latency_seconds_bucket[5m])) by (le))"
        }
      ]
    },
    {
      "type": "stat",
      "title": "Kernel Throughput",
      "targets": [
        {
          "expr": "sum(rate(telecom_networks_requests_total{endpoint='/telecom_networks/kernel'}[5m]))"
        }
      ]
    }
  ]
}
