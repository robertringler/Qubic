apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: market-valuation-ingress-only
  namespace: market-valuation
spec:
  podSelector:
    matchLabels:
      app: market-valuation-backend
  policyTypes: [Ingress, Egress]
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              name: ingress-nginx
      ports:
        - protocol: TCP
          port: 8000
  egress:
    - to:
        - ipBlock:
            cidr: 0.0.0.0/0
      ports:
        - protocol: TCP
          port: 443

