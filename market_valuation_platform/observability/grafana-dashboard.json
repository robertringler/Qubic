{
  "title": "Market Valuation Kernel Observability",
  "panels": [
    {
      "type": "graph",
      "title": "Kernel p95 Latency",
      "targets": [
        {
          "expr": "histogram_quantile(0.95, sum(rate(market_valuation_request_latency_seconds_bucket[5m])) by (le))"
        }
      ]
    },
    {
      "type": "stat",
      "title": "Kernel Throughput",
      "targets": [
        {
          "expr": "sum(rate(market_valuation_requests_total{endpoint='/market_valuation/kernel'}[5m]))"
        }
      ]
    }
  ]
}
