<source>
  @type forward
  port 24224
  bind 0.0.0.0
</source>

<match qratum.**>
  @type file
  path /fluentd/log/qratum
  append true
  <buffer>
    flush_interval 10s
  </buffer>
  <format>
    @type json
  </format>
</match>

<match **>
  @type stdout
</match>
