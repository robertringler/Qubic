{
  "title": "Optimization Services Kernel Observability",
  "panels": [
    {
      "type": "graph",
      "title": "Kernel p95 Latency",
      "targets": [
        {
          "expr": "histogram_quantile(0.95, sum(rate(optimization_services_request_latency_seconds_bucket[5m])) by (le))"
        }
      ]
    },
    {
      "type": "stat",
      "title": "Kernel Throughput",
      "targets": [
        {
          "expr": "sum(rate(optimization_services_requests_total{endpoint='/optimization_services/kernel'}[5m]))"
        }
      ]
    }
  ]
}
