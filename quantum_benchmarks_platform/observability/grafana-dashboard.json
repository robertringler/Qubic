{
  "title": "Quantum Benchmarks Kernel Observability",
  "panels": [
    {
      "type": "graph",
      "title": "Kernel p95 Latency",
      "targets": [
        {
          "expr": "histogram_quantile(0.95, sum(rate(quantum_benchmarks_request_latency_seconds_bucket[5m])) by (le))"
        }
      ]
    },
    {
      "type": "stat",
      "title": "Kernel Throughput",
      "targets": [
        {
          "expr": "sum(rate(quantum_benchmarks_requests_total{endpoint='/quantum_benchmarks/kernel'}[5m]))"
        }
      ]
    }
  ]
}
