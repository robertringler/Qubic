{
  "title": "Finance Trading Kernel Observability",
  "panels": [
    {
      "type": "graph",
      "title": "Kernel p95 Latency",
      "targets": [
        {
          "expr": "histogram_quantile(0.95, sum(rate(finance_trading_request_latency_seconds_bucket[5m])) by (le))"
        }
      ]
    },
    {
      "type": "stat",
      "title": "Kernel Throughput",
      "targets": [
        {
          "expr": "sum(rate(finance_trading_requests_total{endpoint='/finance_trading/kernel'}[5m]))"
        }
      ]
    }
  ]
}
