image:
  repository: <ECR_REPOSITORY_URL>
  tag: latest
service:
  port: 80
ingress:
  enabled: true
  className: nginx
  hosts:
    - host: autonomous_systems.quasim.example.com
      paths:
        - path: /
          pathType: Prefix
  tls:
    - hosts:
        - autonomous_systems.quasim.example.com
      secretName: autonomous-systems-tls
resources:
  requests:
    cpu: 500m
    memory: 1Gi
  limits:
    cpu: 2
    memory: 4Gi
