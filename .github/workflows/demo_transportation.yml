name: Demo - Transportation

on:
  push:
    branches: [main, copilot/*]
    paths:
      - 'quasim/demos/transportation/**'
      - 'quasim/common/**'
      - 'quasim/viz/**'
      - '.github/workflows/demo_transportation.yml'
  pull_request:
    paths:
      - 'quasim/demos/transportation/**'
      - 'quasim/common/**'
      - 'quasim/viz/**'
  workflow_dispatch:

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          
      - name: Install dependencies
        run: |
          pip install -q ruff
          
      - name: Run ruff
        run: |
          ruff check quasim/demos/transportation/
          
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          
      - name: Install dependencies
        run: |
          pip install -q -e ".[dev]"
          
      - name: Run tests
        run: |
          pytest quasim/demos/transportation/tests/ -v --tb=short
          
  build:
    name: Build Artifacts
    runs-on: ubuntu-latest
    needs: [lint, test]
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          
      - name: Install dependencies
        run: |
          pip install -q -e ".[dev]"
          
      - name: Run transportation demo
        run: |
          python -m quasim.demos.transportation.cli plan --steps 100 --seed 42
          
      - name: Verify artifacts
        run: |
          test -f artifacts/transportation/run_42/metrics.json || exit 1
          test -f artifacts/transportation/run_42/log.jsonl || exit 1
          echo "✅ Artifacts verified"
          
      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: transportation-demo-artifacts
          path: artifacts/transportation/
          retention-days: 30
