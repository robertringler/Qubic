image:
  repository: <ECR_REPOSITORY_URL>
  tag: latest
service:
  port: 80
ingress:
  enabled: true
  className: nginx
  hosts:
    - host: healthcare_analytics.quasim.example.com
      paths:
        - path: /
          pathType: Prefix
  tls:
    - hosts:
        - healthcare_analytics.quasim.example.com
      secretName: healthcare-analytics-tls
resources:
  requests:
    cpu: 500m
    memory: 1Gi
  limits:
    cpu: 2
    memory: 4Gi

