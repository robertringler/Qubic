apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: healthcare-analytics-ingress-only
  namespace: healthcare-analytics
spec:
  podSelector:
    matchLabels:
      app: healthcare-analytics-backend
  policyTypes: [Ingress, Egress]
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              name: ingress-nginx
      ports:
        - protocol: TCP
          port: 8000
  egress:
    - to:
        - ipBlock:
            cidr: 0.0.0.0/0
      ports:
        - protocol: TCP
          port: 443

